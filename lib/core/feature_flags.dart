// ...existing code...
class FeatureFlags {
  // Toggle to switch to the new keep-alive PlaceInput widgets.
  // Default true for rollout; can be extended to persist in SharedPreferences.
  static bool keepAlivePlaceInputs = true;
}
